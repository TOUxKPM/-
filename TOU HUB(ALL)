-- TOU HUB (ALL) - Advanced Exploit GUI 🖤💀
-- LocalScript (Exploit Ready - Single File) 🔥 Added Animation Mimic!

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ========== Premium Whitelist ==========
local PremiumUsers = {
	"SUN_lGG65",
	-- เพิ่มชื่อผู้ใช้พรีเมียมตรงนี้ได้ง่าย ๆ
}

local isPremium = table.find(PremiumUsers, player.Name) ~= nil

-- ========== Custom Notification System ==========
local NotifyGui = Instance.new("ScreenGui")
NotifyGui.Name = "TOU_NotifyGui"
NotifyGui.ResetOnSpawn = false
NotifyGui.DisplayOrder = 999999
NotifyGui.Parent = playerGui

local CurrentNotify = nil

local function ShowNotify(text, isError)
	if CurrentNotify and CurrentNotify.Parent then
		CurrentNotify:Destroy()
	end
	
	local NotifyFrame = Instance.new("Frame")
	NotifyFrame.Size = UDim2.new(0, 300, 0, 60)
	NotifyFrame.Position = UDim2.new(0.5, -150, 0, -80)
	NotifyFrame.BackgroundColor3 = isError and Color3.fromRGB(170, 0, 0) or Color3.fromRGB(20, 20, 25)
	NotifyFrame.BorderSizePixel = 0
	NotifyFrame.Parent = NotifyGui
	
	Instance.new("UICorner", NotifyFrame).CornerRadius = UDim.new(0, 12)
	
	local NotifyLabel = Instance.new("TextLabel")
	NotifyLabel.Size = UDim2.new(1, -20, 1, 0)
	NotifyLabel.Position = UDim2.new(0, 10, 0, 0)
	NotifyLabel.BackgroundTransparency = 1
	NotifyLabel.Text = text
	NotifyLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
	NotifyLabel.Font = Enum.Font.GothamBold
	NotifyLabel.TextSize = 18
	NotifyLabel.TextWrapped = true
	NotifyLabel.Parent = NotifyFrame
	
	NotifyFrame.Visible = true
	TweenService:Create(NotifyFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
		Position = UDim2.new(0.5, -150, 0, 20)
	}):Play()
	TweenService:Create(NotifyFrame, TweenInfo.new(0.3, Enum.EasingStyle.Linear), {
		BackgroundTransparency = 0
	}):Play()
	TweenService:Create(NotifyLabel, TweenInfo.new(0.3, Enum.EasingStyle.Linear), {
		TextTransparency = 0
	}):Play()
	
	CurrentNotify = NotifyFrame
	
	task.delay(2, function()
		if NotifyFrame and NotifyFrame.Parent then
			TweenService:Create(NotifyFrame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
				Position = UDim2.new(0.5, -150, 0, -80)
			}):Play()
			TweenService:Create(NotifyFrame, TweenInfo.new(0.4, Enum.EasingStyle.Linear), {
				BackgroundTransparency = 1
			}):Play()
			TweenService:Create(NotifyLabel, TweenInfo.new(0.4, Enum.EasingStyle.Linear), {
				TextTransparency = 1
			}):Play()
			
			task.delay(0.5, function()
				if NotifyFrame and NotifyFrame.Parent then
					NotifyFrame:Destroy()
					if CurrentNotify == NotifyFrame then
						CurrentNotify = nil
					end
				end
			end)
		end
	end)
end

-- ========== Toggle Button (ลอยบนสุด) ==========
local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "TOU_ToggleGui"
ToggleGui.ResetOnSpawn = false
ToggleGui.DisplayOrder = 999999
ToggleGui.IgnoreGuiInset = true
ToggleGui.Parent = playerGui

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "ToggleBtn"
ToggleButton.Size = UDim2.new(0, 64, 0, 64)
ToggleButton.Position = UDim2.new(0, 10, 0.5, -32)
ToggleButton.BackgroundTransparency = 1
ToggleButton.Image = "rbxassetid://91219883240615"
ToggleButton.ZIndex = 10
ToggleButton.Parent = ToggleGui

local dragging = false
local dragStart, startPos

ToggleButton.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = ToggleButton.Position
	end
end)

ToggleButton.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - dragStart
		ToggleButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- ========== Main GUI ==========
local MainGui = Instance.new("ScreenGui")
MainGui.Name = "TOU_HUB_ALL"
MainGui.ResetOnSpawn = false
MainGui.DisplayOrder = 5
MainGui.Parent = playerGui

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 420, 0, 320)
MainFrame.Position = UDim2.new(1, -440, 0, 20)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.Parent = MainGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 40)
TitleBar.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
TitleBar.Parent = MainFrame

Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 14)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -80, 1, 0)
TitleLabel.Position = UDim2.new(0, 15, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "TOU HUB (ALL)"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 18
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -40, 0, 5)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.Text = "−"
MinimizeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.TextSize = 24
MinimizeBtn.Parent = TitleBar

MinimizeBtn.MouseButton1Click:Connect(function()
	MainFrame.Visible = false
end)

local guiDragging = false
local guiDragStart, guiStartPos

TitleBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		guiDragging = true
		guiDragStart = input.Position
		guiStartPos = MainFrame.Position
	end
end)

TitleBar.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		guiDragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if guiDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - guiDragStart
		MainFrame.Position = UDim2.new(guiStartPos.X.Scale, guiStartPos.X.Offset + delta.X, guiStartPos.Y.Scale, guiStartPos.Y.Offset + delta.Y)
	end
end)

-- ========== Tabs ==========
local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(1, 0, 0, 40)
TabFrame.Position = UDim2.new(0, 0, 0, 40)
TabFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
TabFrame.Parent = MainFrame

local TabList = Instance.new("UIListLayout", TabFrame)
TabList.FillDirection = Enum.FillDirection.Horizontal
TabList.Padding = UDim.new(0, 4)
TabList.HorizontalAlignment = Enum.HorizontalAlignment.Left

local function CreateTab(name)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 120, 1, 0)
	btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
	btn.Text = name
	btn.TextColor3 = Color3.fromRGB(180, 180, 180)
	btn.Font = Enum.Font.GothamSemibold
	btn.TextSize = 16
	btn.Parent = TabFrame
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
	return btn
end

local HomeTab = CreateTab("หน้าหลัก")
local ScriptTab = CreateTab("Script")
local UpdateTab = CreateTab("ประกาศอัปเดต")

-- ========== Pages ==========
local PagesFolder = Instance.new("Folder")
PagesFolder.Name = "Pages"
PagesFolder.Parent = MainFrame

local function CreatePage(name)
	local frame = Instance.new("Frame")
	frame.Name = name.."Page"
	frame.Size = UDim2.new(1, -20, 1, -100)
	frame.Position = UDim2.new(0, 10, 0, 90)
	frame.BackgroundTransparency = 1
	frame.Visible = false
	frame.Parent = PagesFolder
	return frame
end

local HomePage = CreatePage("Home")
local ScriptPage = CreatePage("Script")
local UpdatePage = CreatePage("Update")

local WelcomeLabel = Instance.new("TextLabel")
WelcomeLabel.Size = UDim2.new(1, 0, 0, 50)
WelcomeLabel.Position = UDim2.new(0, 0, 0, 20)
WelcomeLabel.BackgroundTransparency = 1
WelcomeLabel.Text = "Welcome to TOU HUB (ALL)"
WelcomeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
WelcomeLabel.Font = Enum.Font.GothamBold
WelcomeLabel.TextSize = 24
WelcomeLabel.Parent = HomePage

local UpdateLabel = Instance.new("TextLabel")
UpdateLabel.Size = UDim2.new(1, -20, 1, -20)
UpdateLabel.Position = UDim2.new(0, 10, 0, 10)
UpdateLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
UpdateLabel.Text = [[
🔥 อัปเดตล่าสุด 🔥
- เพิ่มปุ่ม "เลียนแบบท่าทาง" (mimic animation)
- ข้อมูลผู้เล่น + ฟีเจอร์อื่น ๆ ยังอยู่ครบ

เลียนแบบอนิเมชั่นคนอื่นได้แบบเนียน ๆ 😈💀
]]
UpdateLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
UpdateLabel.Font = Enum.Font.Gotham
UpdateLabel.TextSize = 16
UpdateLabel.TextWrapped = true
UpdateLabel.TextYAlignment = Enum.TextYAlignment.Top
UpdateLabel.Parent = UpdatePage

Instance.new("UICorner", UpdateLabel).CornerRadius = UDim.new(0, 10)

local ScriptScroll = Instance.new("ScrollingFrame")
ScriptScroll.Size = UDim2.new(1, 0, 1, 0)
ScriptScroll.BackgroundTransparency = 1
ScriptScroll.ScrollBarThickness = 6
ScriptScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
ScriptScroll.Parent = ScriptPage

local ScriptGrid = Instance.new("UIGridLayout", ScriptScroll)
ScriptGrid.CellSize = UDim2.new(0, 180, 0, 60)
ScriptGrid.CellPadding = UDim2.new(0, 12, 0, 12)
ScriptGrid.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- ========== Mini Popup GUI ==========
local PopupGui = Instance.new("ScreenGui")
PopupGui.Name = "TOU_PopupGui"
PopupGui.ResetOnSpawn = false
PopupGui.DisplayOrder = 10
PopupGui.Parent = playerGui

local PopupFrame = Instance.new("Frame")
PopupFrame.Size = UDim2.new(0, 360, 0, 220)
PopupFrame.Position = UDim2.new(0.5, -180, 0.5, -110)
PopupFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
PopupFrame.Visible = false
PopupFrame.Parent = PopupGui

Instance.new("UICorner", PopupFrame).CornerRadius = UDim.new(0, 16)

local PopupTitleBar = Instance.new("Frame")
PopupTitleBar.Size = UDim2.new(1, 0, 0, 40)
PopupTitleBar.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
PopupTitleBar.Parent = PopupFrame

Instance.new("UICorner", PopupTitleBar).CornerRadius = UDim.new(0, 16)

local PopupTitleLabel = Instance.new("TextLabel")
PopupTitleLabel.Size = UDim2.new(1, -20, 1, 0)
PopupTitleLabel.Position = UDim2.new(0, 10, 0, 0)
PopupTitleLabel.BackgroundTransparency = 1
PopupTitleLabel.Text = "Script Name"
PopupTitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
PopupTitleLabel.Font = Enum.Font.GothamBold
PopupTitleLabel.TextSize = 18
PopupTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
PopupTitleLabel.Parent = PopupTitleBar

local DescLabel = Instance.new("TextLabel")
DescLabel.Size = UDim2.new(1, -30, 0, 80)
DescLabel.Position = UDim2.new(0, 15, 0, 50)
DescLabel.BackgroundTransparency = 1
DescLabel.Text = "Description here..."
DescLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
DescLabel.Font = Enum.Font.Gotham
DescLabel.TextSize = 15
DescLabel.TextWrapped = true
DescLabel.TextYAlignment = Enum.TextYAlignment.Top
DescLabel.Parent = PopupFrame

local RunBtn = Instance.new("TextButton")
RunBtn.Size = UDim2.new(0, 140, 0, 50)
RunBtn.Position = UDim2.new(0, 20, 1, -70)
RunBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
RunBtn.Text = "▶️ รัน"
RunBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
RunBtn.Font = Enum.Font.GothamBold
RunBtn.TextSize = 18
RunBtn.Parent = PopupFrame
Instance.new("UICorner", RunBtn).CornerRadius = UDim.new(0, 10)

local CancelBtn = Instance.new("TextButton")
CancelBtn.Size = UDim2.new(0, 140, 0, 50)
CancelBtn.Position = UDim2.new(1, -160, 1, -70)
CancelBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
CancelBtn.Text = "❌ ยกเลิก"
CancelBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CancelBtn.Font = Enum.Font.GothamBold
CancelBtn.TextSize = 18
CancelBtn.Parent = PopupFrame
Instance.new("UICorner", CancelBtn).CornerRadius = UDim.new(0, 10)

local popupDragging = false
local popupDragStart, popupStartPos

PopupTitleBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		popupDragging = true
		popupDragStart = input.Position
		popupStartPos = PopupFrame.Position
	end
end)

PopupTitleBar.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		popupDragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if popupDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - popupDragStart
		PopupFrame.Position = UDim2.new(popupStartPos.X.Scale, popupStartPos.X.Offset + delta.X, popupStartPos.Y.Scale, popupStartPos.Y.Offset + delta.Y)
	end
end)

local tweenInfo = TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
local fadeIn = {BackgroundTransparency = 0; Size = UDim2.new(0, 360, 0, 220)}
local fadeOut = {BackgroundTransparency = 1; Size = UDim2.new(0, 340, 0, 200)}

local function OpenPopup()
	PopupFrame.Visible = true
	PopupFrame.Size = UDim2.new(0, 340, 0, 200)
	PopupFrame.BackgroundTransparency = 1
	TweenService:Create(PopupFrame, tweenInfo, fadeIn):Play()
	RunBtn.Text = "▶️ รัน"
	RunBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
	RunBtn.Active = true
end

local function ClosePopup()
	TweenService:Create(PopupFrame, tweenInfo, fadeOut):Play()
	task.delay(0.4, function()
		PopupFrame.Visible = false
	end)
end

-- ========== Script Template System ==========
local Scripts = {
	{
		Name = "บิน",
		ButtonName = "บิน",
		Description = "script บิน",
		Function = function()
			loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Gui-Fly-v3-37111"))()
		end
	},
	{
		Name = "ความเร็ว",
		ButtonName = "ความเร็ว",
		Description = "script เพิ่มความเร็ว",
		Function = function()
			loadstring(game:HttpGet("https://pastefy.app/cCNFO7iL/raw"))()
		end
	},
	{
		Name = "ESP Players",
		ButtonName = "ESP Players",
		Description = "script มองเลือด, ความห่างของผู้เล่น,\nชื่อจริง (ในเกม) และแสดง hitbox",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/9451b42da05abea85433ef5431790932"))()
		end
	},
	{
		Name = "กระโดดไม่จำกัด",
		ButtonName = "กระโดดไม่จำกัด",
		Description = "กระโดดไม่จำกัด",
		Function = function()
			loadstring(game:HttpGet("https://pastefy.app/OtfK65dQ/raw"))()
		end
	},
	{
		Name = "Shift Lock",
		ButtonName = "Shift Lock",
		Description = "shift lock เหมือนในคอม🥵",
		Function = function()
			loadstring(game:HttpGet("https://pastefy.app/DhiytJbS/raw"))()
		end
	},
	{
		Name = "infiniteyield",
		ButtonName = "Infinite Yield",
		Description = "มีคำสั่งเยอะอยู่👀",
		Function = function()
			loadstring(game:HttpGet(('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'), true))()
		end
	},
	{
		Name = "กระโดด",
		ButtonName = "กระโดด",
		Description = "ไว้ปรับการกระโดด ว่าจะกระโดดสูงแค่ไหน",
		Function = function()
			loadstring(game:HttpGet("https://pastefy.app/bv5KayX6/raw"))()
		end
	},
	{
		Name = "R6",
		ButtonName = "R6",
		Description = "ปรับจาก R15 ไป R6",
		Function = function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/Imagnir/r6_anims_for_r15/main/r6_anims.lua", true))()
		end
	},
	{
		Name = "เพิ่ม FPS",
		ButtonName = "เพิ่ม FPS",
		Description = "ทำให้เกมลื่นขึ้น และมียืดจอ",
		Function = function()
			loadstring(game:HttpGet("https://pastefy.app/WmJoyaBZ/raw"))()
		end
	},
	{
		Name = "ซุ่มมอง",
		ButtonName = "ซุ่มมอง",
		Description = "ดูคนอื่นแบบ ที่คนอื่นไม่รู้ตัว🤪",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/56ebde3a61f4a943773b967541428290"))()
		end
	},
	{
		Name = "ต่อต้านเข้าหลัง",
		ButtonName = "ต่อต้านเข้าหลัง",
		Description = "เมื่อเปิด พวกโปร bang หรือพวกวาปตามจะตายทันที🤪",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/cf443c14410602da23a3adeb12eadc21"))()
		end
	},
	{
		Name = "กระเด็นคน",
		ButtonName = "กระเด็นคน",
		Description = "กระเด็นผู้เล่นในเกมได้ (ใช้เฉพาะแมพที่ชนกันได้)🥶",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/67d4bac5bb5c40bbac2a11a33bc71db0"))()
		end
	},
	{
		Name = "ท่าเต้น (R15)",
		ButtonName = "ท่าเต้น (R15)",
		Description = "ท่าเต้นมีเกือบทุกท่ามั้ง (R15)",
		Function = function()
			loadstring(game:HttpGet("https://raw.githubusercontent.com/7yd7/Hub/refs/heads/Branch/GUIS/Emotes.lua"))()
		end
	},
	{
		Name = "ขยาย hitbox",
		ButtonName = "ขยาย hitbox",
		Description = "ขยาย hitbox ทำให้ตีไกลขึ้น",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/758017cf4e6ffa8fed43174fb3d43057"))()
		end
	},
	{
		Name = "แจ้งเตือน",
		ButtonName = "แจ้งเตือน",
		Description = "จะแจ้งเตือนคนที่เข้า-ออกเกม\nพิมพ์ /ปิด เพื่อปิดการแจ้งเตือน\nพิมพ์ /เปิด เพื่อเปิดการแจ้งเตือนอีกครั้ง",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/9f7f1fc40cf73bb516da640dee5990aa"))()
		end
	},
	{
		Name = "ดึงผู้เล่น",
		ButtonName = "ดึงผู้เล่น",
		Description = "ดึงผู้เล่นคนในเซิร์ฟได้ หรือดึงทุกคนในเซิร์ฟก็ได้ (เห็นเฉพาะมุมมองของเรา)",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/50936d24612b4f1de50b2af58c524645"))()
		end
	},
	{
		Name = "ล็อคผู้เล่น",
		ButtonName = "ล็อคผู้เล่น",
		Description = "กล้องจะล็อคไปหาผู้เล่นหรือ NPC\nมี 3 โหมดให้เลือก:\n1) ใกล้สุด\n2) ไกลสุด\n3) กล้อง",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/f8bb866c899b4e0b8e0a6d80b756aa0e"))()
		end
	},
	{
		Name = "วาปหาผู้เล่น",
		ButtonName = "วาปหาผู้เล่น",
		Description = "วาปไปหาผู้เล่นทุกคนในเซิร์ฟเวอร์ได้",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/e3cb0381a6b84f240b7c691eaf48a10b"))()
		end
	},
	{
		Name = "ออโต้ใช้ไอเท็ม",
		ButtonName = "ออโต้ใช้ไอเท็ม",
		Description = "จะใช้งานไอเท็ม (Tool) แบบออโต้",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/388a61ce9260266fc8785623ae4554b5"))()
		end
	},
	{
		Name = "เพิ่มแสง",
		ButtonName = "เพิ่มแสง",
		Description = "เพิ่มแสงในเกมให้สว่าง\nคำสั่ง:\n/เปิดfb = เปิดแสง\n/ปิดfb = ปิดแสง",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/69125f75192aa359c409f8006b682f0e"))()
		end
	},
	{
		Name = "ออโต้หยิบของ",
		ButtonName = "ออโต้หยิบของ",
		Description = "หยิบไอเท็ม (Tool) อัตโนมัติ\nใช้ได้เฉพาะบางแมพที่ไอเท็มสามารถดรอปได้",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/e241fb6c78cc877b2782e0e7fbd3b9a6"))()
		end
	},
	{
		Name = "เช็ค FPS",
		ButtonName = "เช็ค FPS",
		Description = "เช็คว่า FPS ของคุณเยอะหรือน้อย",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/252eb737c013b20598e22c268142cc8c"))()
		end
	},
	{
		Name = "AntiLogger",
		ButtonName = "AntiLogger",
		Description = "ป้องกันการขโมยเสียง จากสคริปต์ขโมยเสียง (Logger)",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/21c9f7cd18d1829ee29492fcd0e0b73e"))()
		end,
		Premium = true
	},
	{
		Name = "TP Walk",
		ButtonName = "TP Walk",
		Description = "จะเปลี่ยนจากการเดิน เป็นการพุ่งไปข้างหน้า",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/7e141d8be6e392cda83a4e635febfa58"))()
		end
	},
	{
		Name = "หาข้อมูลเพลง",
		ButtonName = "หาข้อมูลเพลง",
		Description = "ดูได้ว่าคนสร้างเพลงคือใคร และสามารถเซฟเก็บไว้ได้",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/d99b3f9859787949ab8ff119e528c97d"))()
		end
	},
	{
		Name = "ข้อมูลผู้เล่น",
		ButtonName = "ข้อมูลผู้เล่น",
		Description = "ดูได้ว่าผู้เล่นอยู่ห่างแค่ไหน\nตรวจสอบอายุบัญชี\nมีปุ่มเสริม: เดินตาม / แช่แข็ง / หันมอง",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/b8a6bfd8d85c5fa04d67a95e02d843a2"))()
		end
	},
	{
		Name = "เลียนแบบท่าทาง",
		ButtonName = "เลียนแบบท่าทาง",
		Description = "เลียนแบบอนิเมชั่นคนอื่นได้",
		Function = function()
			loadstring(game:HttpGet("https://protected-roblox-scripts.onrender.com/cd0d35ea9247e8832900d224f0c0edd0"))()
		end
	},
}

for _, scriptData in ipairs(Scripts) do
	local btn = Instance.new("TextButton")
	btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
	btn.Text = scriptData.ButtonName or scriptData.Name
	btn.TextColor3 = Color3.fromRGB(220, 220, 220)
	btn.Font = Enum.Font.GothamSemibold
	btn.TextSize = 16
	btn.TextWrapped = true
	btn.Parent = ScriptScroll
	
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
	
	if scriptData.Premium then
		task.spawn(function()
			local hue = 0
			while btn and btn.Parent do
				hue = (hue + 1) % 360
				btn.BackgroundColor3 = Color3.fromHSV(hue / 360, 1, 1)
				task.wait(0.1)
			end
		end)
	end
	
	btn.MouseButton1Click:Connect(function()
		PopupTitleLabel.Text = scriptData.Name
		DescLabel.Text = scriptData.Description
		
		MainFrame.Visible = false
		OpenPopup()
		
		local cancelConn
		cancelConn = CancelBtn.MouseButton1Click:Connect(function()
			ClosePopup()
			MainFrame.Visible = true
			cancelConn:Disconnect()
		end)
		
		local runConn
		runConn = RunBtn.MouseButton1Click:Connect(function()
			if not RunBtn.Active then return end
			
			ClosePopup()
			MainFrame.Visible = true
			
			if scriptData.Premium and not isPremium then
				ShowNotify("ไปซื้อจากเจ้าของสคริปต์ก่อน")
				runConn:Disconnect()
				if cancelConn then cancelConn:Disconnect() end
				return
			end
			
			ShowNotify("กำลังรัน " .. scriptData.Name .. "... ⏳")
			
			RunBtn.Text = "รันแล้ว ✅"
			RunBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
			RunBtn.Active = false
			
			task.spawn(function()
				local success, err = pcall(scriptData.Function)
				if success then
					if scriptData.Name == "เลียนแบบท่าทาง" then
						ShowNotify("เปิดระบบเลียนแบบท่าทาง")
					elseif scriptData.Name == "ข้อมูลผู้เล่น" then
						ShowNotify("เปิดระบบข้อมูลผู้เล่นแล้ว")
					elseif scriptData.Name == "หาข้อมูลเพลง" then
						ShowNotify("เปิดระบบหาข้อมูลเพลงแล้ว")
					elseif scriptData.Name == "TP Walk" then
						ShowNotify("เปิดระบบ TP Walk แล้ว")
					elseif scriptData.Name == "เช็ค FPS" then
						ShowNotify("เช็ค FPS เรียบร้อยแล้ว")
					elseif scriptData.Name == "ออโต้หยิบของ" then
						ShowNotify("เปิดระบบออโต้หยิบของแล้ว")
					elseif scriptData.Name == "เพิ่มแสง" then
						ShowNotify("เปิดระบบเพิ่มแสงแล้ว")
					elseif scriptData.Name == "ออโต้ใช้ไอเท็ม" then
						ShowNotify("เปิดระบบออโต้ใช้ไอเท็มแล้ว")
					elseif scriptData.Name == "วาปหาผู้เล่น" then
						ShowNotify("วาปหาผู้เล่นเรียบร้อยแล้ว")
					elseif scriptData.Name == "ล็อคผู้เล่น" then
						ShowNotify("เปิดระบบล็อคผู้เล่นแล้ว")
					elseif scriptData.Name == "ดึงผู้เล่น" then
						ShowNotify("เปิดระบบดึงผู้เล่นแล้ว")
					elseif scriptData.Name == "แจ้งเตือน" then
						ShowNotify("เปิดระบบแจ้งเตือนเข้า-ออกเกมแล้ว")
					elseif scriptData.Name == "ขยาย hitbox" then
						ShowNotify("เปิดโหมดขยาย hitbox แล้ว")
					elseif scriptData.Name == "ท่าเต้น (R15)" then
						ShowNotify("เปิดเมนูท่าเต้น (R15) เรียบร้อยแล้ว")
					elseif scriptData.Name == "กระเด็นคน" then
						ShowNotify("เปิดโหมดกระเด็นคนแล้ว")
					elseif scriptData.Name == "ต่อต้านเข้าหลัง" then
						ShowNotify("เปิดโหมดต่อต้านเข้าหลังแล้ว")
					elseif scriptData.Name == "ซุ่มมอง" then
						ShowNotify("เปิดโหมดซุ่มมองเรียบร้อยแล้ว")
					elseif scriptData.Name == "เพิ่ม FPS" then
						ShowNotify("เพิ่ม FPS เรียบร้อยแล้ว")
					elseif scriptData.Name == "R6" then
						ShowNotify("ปรับเป็น R6 เรียบร้อยแล้ว")
					elseif scriptData.Name == "กระโดด" then
						ShowNotify("รันสคริปต์กระโดดเรียบร้อยแล้ว")
					elseif scriptData.Name == "infiniteyield" then
						ShowNotify("รัน Infinite Yield สำเร็จแล้ว")
					else
						ShowNotify(scriptData.Name .. " รันสำเร็จแล้ว! 🚀")
					end
				else
					ShowNotify("รันล้มเหลว: " .. tostring(err), true)
				end
			end)
			
			runConn:Disconnect()
			if cancelConn then cancelConn:Disconnect() end
		end)
	end)
end

-- ========== Tab System ==========
local currentPage = HomePage

local function ShowPage(page)
	if currentPage then currentPage.Visible = false end
	page.Visible = true
	currentPage = page
end

local function SelectTab(tabBtn)
	for _, btn in pairs(TabFrame:GetChildren()) do
		if btn:IsA("TextButton") then
			if btn == tabBtn then
				TweenService:Create(btn, tweenInfo, {BackgroundColor3 = Color3.fromRGB(0, 120, 215)}):Play()
				TweenService:Create(btn, tweenInfo, {TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
			else
				TweenService:Create(btn, tweenInfo, {BackgroundColor3 = Color3.fromRGB(30, 30, 35)}):Play()
				TweenService:Create(btn, tweenInfo, {TextColor3 = Color3.fromRGB(180, 180, 180)}):Play()
			end
		end
	end
end

HomeTab.MouseButton1Click:Connect(function() ShowPage(HomePage) SelectTab(HomeTab) end)
ScriptTab.MouseButton1Click:Connect(function() ShowPage(ScriptPage) SelectTab(ScriptTab) end)
UpdateTab.MouseButton1Click:Connect(function() ShowPage(UpdatePage) SelectTab(UpdateTab) end)

ShowPage(HomePage)
SelectTab(HomeTab)

ToggleButton.MouseButton1Click:Connect(function()
	MainFrame.Visible = not MainFrame.Visible
	if MainFrame.Visible then
		ShowPage(HomePage)
		SelectTab(HomeTab)
	end
end)

print("🔥 TOU HUB (ALL) + Animation Mimic Added! 😈💀🚀")
